# GM roster — who may run the "@" commands (Server/GmAccounts.cs, Server/Commands.cs).
#
# One character name per line. Matching is case-insensitive, same normalization accounts and characters
# use, so "Brian" / "brian" / "BRIAN" are the same GM. Blank lines and #-comments are ignored.
#
# EMPTY = NOBODY. A "@" command from a non-GM gets "Unknown command." and is logged — the same answer a
# typo gets, so the tooling stays invisible rather than merely locked. That is the safe default for an
# internet-facing server: these commands spawn items, set level and stats, warp, mint coins, and
# hot-reload content.
#
# Re-read by @reload as well as at startup, so promoting or demoting someone needs no restart.
# NEXUS_GMS (comma-separated) is unioned with this file, for deployments with no writable data dir.
#
# Add your own character below:
Zaleroo
